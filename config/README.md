brat configuration files used by ECO-CollecTF:

* annotation.conf: The ECO annotation entity was added. Under the [relations] section, the <OVERLAP> setting was added to allow overlapping annotations. Confidence was added as an attribute.
* visual.conf: Added the “DB” called brat_eco with the path to the working directory containing the ontology, and the URL to the online ECO.
* tools.conf: Added glyphs for the attribute values High, Medium, and Low.

